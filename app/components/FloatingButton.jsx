import { Pressable } from "react-native";
// import { PlusButton } from "../../assets/Plus-button";

export default function FloatingButton({ episodeId }) {
  const interactions = () => {};
  return <Pressable onPress={interactions}>{/* <PlusButton />*/}</Pressable>;
}
